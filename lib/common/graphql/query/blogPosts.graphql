query blogPosts($page: Int, $categoryId: String) {
  blogPosts(page: $page, categoryId: $categoryId) {
    items {
      id
      title
      thumbnailSrc {
        md
        original
      }
    }
  }
}
